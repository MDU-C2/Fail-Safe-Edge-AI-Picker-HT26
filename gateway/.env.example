# development = intentionally relaxed local development identity
# strict = fails closed until production authentication is implemented
GATEWAY_SECURITY_MODE=development
GATEWAY_CAMERA_PROVIDER=oak

GATEWAY_CAMERA_FPS=1

GATEWAY_CAMERA_RGB_WIDTH=640
GATEWAY_CAMERA_RGB_HEIGHT=480

GATEWAY_CAMERA_JPEG_QUALITY=90